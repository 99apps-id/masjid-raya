# Termigo agent memory

Written by the Termigo agent as it works. Safe to edit or delete by hand;
removing a line makes the agent forget it. Your own notes belong in
TERMIGO.md, which the agent never rewrites.
- 2026-09-17 [GOTCHA] masjid-raya-pro: node_modules bawaan repo rusak (entri JUNCTION ke node_modules/.pnpm yang tidak bisa dibaca, error 'UNKNOWN: unknown error' / ERROR_UNTRUSTED_MOUNT_POINT). `pnpm install` selalu gagal; perbaikannya: rename node_modules ke node_modules_rusak lalu `npm install` (package-lock.json valid, layout flat tanpa junction).
- 2026-09-18 [GOTCHA] masjid-raya-pro: mount workspace tidak stabil — kadang memunculkan farm symlink pnpm lama di node_modules (entri tak terbaca, errno UNKNOWN), sehingga `next`/`tsc`/`tsx` gagal resolve. `npm install` sering TIDAK menggantinya; pakai `npm ci` (menghapus & memasang ulang dari package-lock.json → layout flat asli). Setelah itu WAJIB hapus folder sisa `node_modules_*` (mis. node_modules_rusak/node_modules_old) karena tsconfig meng-include `**/*.ts` sehingga error tsc dari folder itu membanji
- 2026-09-18 [GOTCHA] masjid-raya-pro: node_modules bisa "kembali rusak" (farm symlink pnpm) setelah idle beberapa menit — build/tsc yang tadinya lolos bisa tiba-tiba gagal resolve `next/dist/...`. Workaround andal: jalankan hapus node_modules + `npm ci` + `npx prisma generate` + `npx next build` + `next start` + smoke test DALAM SATU skrip Node (child_process) sekali jalan, karena jendela sehatnya hanya beberapa menit.
- 2026-09-18 [GOTCHA] masjid-raya-pro: CSP di next.config.mjs menyetel `img-src 'self' data: blob:` (tanpa https:), padahal tautanBerkas() di src/lib/validasi.ts mengizinkan URL gambar https://. Akibatnya gambar/logo yang diisi via tautan https tersimpan tapi diblokir diam-diam oleh CSP.
- 2026-09-18 masjid-raya-pro: kebijakan akses FINAL (menggantikan gotcha lama) — Ustadz, Pengurus, Profil, Pengaturan = admin-only, dikunci di API via withAdminAuth(handler, ["admin"]) dan dijaga komponen server `src/components/admin/ProteksiAdmin.tsx` lewat layout per-subtree di src/app/admin/{ustadz,pengurus,profil,pengaturan}/layout.tsx. Pengurus hanya kelola konten: Jadwal, Khutbah, Kegiatan, Berita.
- 2026-09-18 masjid-raya-pro: pengaturan baru `hijriah_offset_hari` (-2..+2, default "0") menggeser tanggal Hijriah agar selaras kalender Kemenag (Umm al-Qura sering beda 1 hari). Di-thread: getPetaPengaturan -> getJadwalHarian(OpsiJadwal.hijriahOffsetHari) -> waktu.hijriahLokal/bulanHijriah(offsetHari) -> papan.ringkasPapan(...,hijriahOffsetHari) -> prop hijriahOffsetHari pada DisplayBoard. CSP img-src kini menyertakan https: agar gambar tautan https tidak diblokir.
